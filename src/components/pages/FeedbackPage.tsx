import { useState } from 'react'; import { CheckCircle2, MessageSquare, Send, Star } from 'lucide-react'; import { toast } from 'sonner';
import { SEOHead } from '../utility/SEOHead'; import { PublicFormLayout } from '../pieces/general/PublicFormLayout'; import { Input } from '../ui/input'; import { Textarea } from '../ui/textarea'; import { Button } from '../ui/button'; import { Label } from '../ui/label'; import { useHomeStore } from '../../libs/store/home.store';
interface Props { onNavigate: (page: string) => void }
export function FeedbackPage({ onNavigate }: Props) { const submitFeedback = useHomeStore((state) => state.submitFeedback); const [form, setForm] = useState({ name: '', email: '', category: 'general', message: '', rating: 0 }); const [loading, setLoading] = useState(false); const [sent, setSent] = useState(false); async function handleSubmit(e: React.FormEvent) { e.preventDefault(); if (!form.rating) { toast.error('Please choose a star rating.'); return; } setLoading(true); try { await submitFeedback({ name: form.name, email: form.email, rating: form.rating, message: form.message }); setSent(true); toast.success('Thank you for your feedback.'); } catch (error) { toast.error(error instanceof Error ? error.message : 'Unable to send feedback.'); } finally { setLoading(false); } } return <><SEOHead title="Share Feedback" description="Help shape Naitrust before launch." canonicalPath="/feedback" /><PublicFormLayout eyebrow="Shape Naitrust" title="Your experience can make the product better." description="Tell us what feels useful, confusing or missing as Naitrust prepares for launch." highlights={['Suggest the transaction flows that matter most', 'Tell us what would increase your confidence', 'Flag unclear language or usability problems']}>{sent ? <div className="py-12 text-center"><CheckCircle2 className="mx-auto text-green-600" size={50} /><h2 className="mt-5 text-2xl font-bold">Thank you</h2><p className="mt-3 text-muted-foreground">Your feedback has been recorded.</p><Button className="mt-7" onClick={() => setSent(false)}>Share more feedback</Button></div> : <><MessageSquare className="text-primary" size={34} /><h2 className="mt-5 text-2xl font-bold">Share your feedback</h2><form onSubmit={handleSubmit} className="mt-7 space-y-5"><div><Label>How was your experience?</Label><div className="mt-2 flex gap-1">{[1,2,3,4,5].map((value) => <button key={value} type="button" aria-label={`${value} stars`} onClick={() => setForm({ ...form, rating: value })} className={`rounded-full p-2 ${form.rating >= value ? 'text-amber-500' : 'text-muted-foreground'}`}><Star size={24} className={form.rating >= value ? 'fill-current' : ''} /></button>)}</div></div><div className="grid gap-4 sm:grid-cols-2"><div><Label>Name</Label><Input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-2" /></div><div><Label>Email</Label><Input type="email" required value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="mt-2" /></div></div><div><Label>Feedback type</Label><select value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} className="mt-2 h-12 w-full rounded-full border-2 bg-input-background px-4"><option value="general">General feedback</option><option value="feature">Product suggestion</option><option value="improvement">Improvement</option><option value="bug">Website issue</option></select></div><div><Label>Your feedback</Label><Textarea required value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} className="mt-2 min-h-40" /></div><Button disabled={loading} type="submit" size="lg" className="w-full">{loading ? 'Submitting…' : <>Submit feedback <Send /></>}</Button><button type="button" onClick={() => onNavigate('home')} className="w-full text-sm font-semibold text-primary">Back to home</button></form></>}</PublicFormLayout></>; }
