# Naitrust — robots.txt
# Public marketing pages are OPEN to all search engines and AI assistants so
# the site can be found and cited. The authenticated app (/app) and the admin
# console (/admin) are private and must never be indexed.
#
# NOTE: AI-crawler blocking (GPTBot, ClaudeBot, etc.) is NOT set here — if AI
# assistants still can't read the site, disable "Block AI bots / AI Scrapers"
# in the Cloudflare dashboard (Security → Bots), which appends its own rules.

User-agent: *
Allow: /
# Private, authenticated areas — keep out of every index for security.
Disallow: /app
Disallow: /admin
# Transient auth/verification flows — no SEO value.
Disallow: /verify-email
Disallow: /verify-code
Disallow: /forgot-password

Sitemap: https://naitrust.com/sitemap.xml
