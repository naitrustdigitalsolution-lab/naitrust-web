/**
 * TeamMemberInterface
 * A team member added while registering a business (name, role, and contact).
 */
export interface TeamMemberInterface {
  name: string;
  role: string;
  email: string;
  phone: string;
}
