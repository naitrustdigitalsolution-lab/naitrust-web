; Several deps (react-helmet-async v2, react-day-picker v8, @react-three/fiber v8)
; cap their React peer range at 18 but work fine on React 19. Allow the install
; to proceed instead of failing on ERESOLVE peer conflicts.
legacy-peer-deps=true
